{% extends "layout.njk" %}

{% block content %}
<main>
    <div class="content">
        {% for item in items %}
        <div class="card">
            <img src="{{item.image}}" alt="{{item.title}}">
            <h4>{{item.title}}</h4>
            <p>por {{item.author}}</p>
        </div>
        {% endfor%}
    </div>
</main>   
{% endblock %}